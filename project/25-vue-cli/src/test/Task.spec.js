//import { shallowMount } from '@vue/test-utils'
import Task from '@/components/Task.vue'

describe('Task.vue', () => {
  it('renders li for each item in props.items', () => {
      console.log("You fucking tester")

    expect(1).toBe(1)
  })

})